# Java backend background, AI beginner

The user has Java development experience but is effectively new to AI development terminology and patterns. Future lessons should introduce AI concepts through Spring Boot/backend analogies first, then map them to the actual Noto implementation, and include enough implementation structure for the user to build a small AI module.
