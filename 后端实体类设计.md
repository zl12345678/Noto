# Noto · 知微 后端实体类设计

## 1. 设计目标
本文件用于定义后端核心实体类、字段、关系和建模原则，确保后续开发时：
- 实体类与数据库结构一致
- 业务边界清晰
- 便于 MyBatis Plus 维护
- 便于后续扩展多端与协作能力

---

## 2. 实体设计原则
1. 实体类与数据库表一一对应
2. 核心字段尽量使用基础类型，避免复杂嵌套
3. AI 生成结果与原始内容分离存储
4. 所有实体继承统一基础字段
5. 逻辑删除、创建时间、更新时间统一处理

---

## 3. 基础父类设计

### 3.1 BaseEntity
所有业务实体建议继承基础父类 `BaseEntity`。

#### 字段
- `id` Long 主键
- `createdAt` LocalDateTime 创建时间
- `updatedAt` LocalDateTime 更新时间
- `deleted` Boolean 逻辑删除

#### 说明
- 方便统一处理审计和软删除
- 由 MyBatis Plus 自动填充或统一拦截处理

---

## 4. 核心实体类设计

## 4.1 UserEntity
对应表：`user`

### 字段
- `username` String
- `email` String
- `passwordHash` String
- `nickname` String
- `avatarUrl` String
- `status` Integer
- `lastLoginAt` LocalDateTime

### 说明
- 用户身份核心实体
- 后续可扩展第三方登录字段

---

## 4.2 WorkspaceEntity
对应表：`workspace`

### 字段
- `ownerUserId` Long
- `name` String
- `type` Integer
- `description` String
- `status` Integer

### 说明
- 当前以个人工作区为主
- 后续支持团队工作区

---

## 4.3 NoteFolderEntity
对应表：`note_folder`

### 字段
- `workspaceId` Long
- `parentId` Long
- `name` String
- `sortOrder` Integer

### 说明
- 支持层级目录
- 适合知识分类管理

---

## 4.4 NoteEntity
对应表：`note`

### 字段
- `workspaceId` Long
- `folderId` Long
- `title` String
- `content` String
- `contentType` String
- `summary` String
- `status` Integer
- `isFavorite` Boolean
- `lastEditedAt` LocalDateTime
- `createdBy` Long
- `updatedBy` Long

### 说明
- 系统最核心实体
- `content` 存原文
- `summary` 存 AI 或人工总结

---

## 4.5 NoteBlockEntity
对应表：`note_block`

### 字段
- `noteId` Long
- `blockIndex` Integer
- `blockType` String
- `content` String
- `tokenCount` Integer
- `embeddingId` String

### 说明
- 用于搜索召回
- 用于 AI 上下文拼装

---

## 4.6 TagEntity
对应表：`tag`

### 字段
- `workspaceId` Long
- `name` String
- `color` String

### 说明
- 标签属于工作区维度
- 建议同工作区内标签名唯一

---

## 4.7 NoteTagEntity
对应表：`note_tag`

### 字段
- `noteId` Long
- `tagId` Long

### 说明
- 多对多关联实体
- 一般只作为中间表使用

---

## 4.8 NoteAttachmentEntity
对应表：`note_attachment`

### 字段
- `noteId` Long
- `fileName` String
- `fileType` String
- `fileSize` Long
- `fileUrl` String
- `storageKey` String

### 说明
- 存储附件元数据
- 实体不直接保存文件内容

---

## 4.9 AiChatSessionEntity
对应表：`ai_chat_session`

### 字段
- `workspaceId` Long
- `userId` Long
- `sessionType` String
- `title` String
- `latestMessageAt` LocalDateTime

### 说明
- 支持多轮问答会话
- 支持不同类型的 AI 场景

---

## 4.10 AiChatMessageEntity
对应表：`ai_chat_message`

### 字段
- `sessionId` Long
- `role` String
- `content` String
- `tokenUsage` Integer
- `modelName` String
- `referencedNoteIds` String

### 说明
- 记录对话消息
- 支持问答回放和调试

---

## 4.11 TodoItemEntity
对应表：`todo_item`

### 字段
- `workspaceId` Long
- `noteId` Long
- `title` String
- `description` String
- `priority` Integer
- `status` Integer
- `dueAt` LocalDateTime
- `completedAt` LocalDateTime
- `createdBy` Long

### 说明
- 支持 AI 提取待办
- 支持用户手动创建待办

---

## 4.12 ReminderEntity
对应表：`reminder`

### 字段
- `workspaceId` Long
- `todoId` Long
- `reminderType` String
- `triggerAt` LocalDateTime
- `message` String
- `status` Integer
- `sentAt` LocalDateTime

### 说明
- 负责提醒触发状态
- 后续可扩展通知通道

---

## 4.13 SearchIndexTaskEntity
对应表：`search_index_task`

### 字段
- `noteId` Long
- `taskType` String
- `status` Integer
- `retryCount` Integer
- `errorMessage` String

### 说明
- 异步构建检索索引
- 可用于失败重试

---

## 4.14 AiTaskEntity
对应表：`ai_task`

### 字段
- `workspaceId` Long
- `userId` Long
- `taskType` String
- `targetId` Long
- `status` Integer
- `inputContent` String
- `outputContent` String
- `retryCount` Integer
- `errorMessage` String

### 说明
- 记录 AI 任务全流程
- 便于追踪 AI 执行过程

---

## 4.15 AuditLogEntity
对应表：`audit_log`

### 字段
- `workspaceId` Long
- `userId` Long
- `actionType` String
- `resourceType` String
- `resourceId` Long
- `detail` String
- `traceId` String

### 说明
- 审计与排查问题使用
- 只写入，不参与业务判断

---

## 4.16 UserSettingEntity
对应表：`user_setting`

### 字段
- `userId` Long
- `settingKey` String
- `settingValue` String

### 说明
- 保存偏好设置
- 适合提醒偏好、AI 偏好、UI 偏好

---

## 5. 枚举设计建议

## 5.1 CommonStatusEnum
可用于：
- 用户状态
- 笔记状态
- 提醒状态
- AI 任务状态
- 搜索任务状态

建议枚举值：
- `0` 正常 / 待处理 / 未开始
- `1` 启用 / 处理中 / 进行中
- `2` 完成
- `3` 失败 / 已过期 / 已取消

---

## 5.2 NoteStatusEnum
建议值：
- `0` 正常
- `1` 归档
- `2` 删除

---

## 5.3 TodoStatusEnum
建议值：
- `0` 未开始
- `1` 进行中
- `2` 已完成
- `3` 已过期

---

## 5.4 PriorityEnum
建议值：
- `0` 低
- `1` 中
- `2` 高
- `3` 紧急

---

## 5.5 AiTaskTypeEnum
建议值：
- `summarize`
- `qa`
- `extract_todo`
- `agent_flow`

---

## 6. DTO / VO / BO 建议

## 6.1 DTO
用于前端传入参数。
示例：
- `NoteCreateRequest`
- `NoteUpdateRequest`
- `TodoCreateRequest`
- `AiAskRequest`

## 6.2 VO
用于对外返回。
示例：
- `NoteDetailVO`
- `TodoItemVO`
- `AiAnswerVO`
- `SearchResultVO`

## 6.3 BO
用于业务层内部处理。
示例：
- `NoteCreateBO`
- `AiSummarizeBO`
- `TodoExtractBO`

---

## 7. 实体设计与开发顺序建议
建议优先实现以下实体：
1. `UserEntity`
2. `WorkspaceEntity`
3. `NoteFolderEntity`
4. `NoteEntity`
5. `TagEntity`
6. `NoteTagEntity`
7. `TodoItemEntity`
8. `ReminderEntity`
9. `AiChatSessionEntity`
10. `AiChatMessageEntity`
11. `AiTaskEntity`
12. `SearchIndexTaskEntity`
13. `AuditLogEntity`
14. `UserSettingEntity`
15. `NoteBlockEntity`
16. `NoteAttachmentEntity`

---

## 8. 建议的包结构
```text
com.noto.zhihui
├── common
├── config
├── controller
├── dto
├── entity
├── enums
├── mapper
├── repository
├── service
├── service.impl
├── vo
├── bo
├── ai
├── task
├── security
└── util
```

---

## 9. 结论
后端实体层建议采用“基础父类 + 核心业务实体 + AI 任务实体 + 审计实体”的方式设计。这样既能满足 MVP 快速开发，也能为后续多端和协作扩展留下足够空间。
