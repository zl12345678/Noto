spring:
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java,src/main/resources
    livereload:
      enabled: true
  thymeleaf:
    cache: false

logging:
  level:
    org.springframework.boot.devtools: info
