# Noto · 知微 OpenAPI 规范
#
# 此文件由 scripts/export-openapi.ps1 从运行中的后端生成。
# 生成命令（后端 dev 启动后）：
#   .\scripts\export-openapi.ps1
#
# 在线文档（开发环境）：
#   http://localhost:9086/swagger-ui.html
#   http://localhost:9086/v3/api-docs.yaml
