# Noto · 知微 后端工程初始化说明

## 1. 初始化目标
后端工程采用 Java 17 + Spring Boot 3 + MyBatis Plus + PostgreSQL + Redis + RabbitMQ + Elasticsearch + MinIO + Spring AI Alibaba 的技术组合，目标是快速搭建一个模块化单体后端，为 Web 端 MVP 提供稳定接口，并为未来多端扩展预留统一能力。

---

## 2. 初始化原则
1. 先做模块化单体，不一开始拆微服务
2. 统一 API、统一异常、统一返回结构
3. 业务模块与 AI 模块解耦
4. 数据层和缓存层各司其职
5. 所有关键动作都要可追踪、可审计

---

## 3. 工程创建建议

### 3.1 项目基础信息
- 项目名：`noto-zhihui-backend`
- 语言：`Java 17`
- 框架：`Spring Boot 3`
- ORM：`MyBatis Plus`
- 数据库：`PostgreSQL`
- 缓存：`Redis`
- 消息队列：`RabbitMQ`
- 检索：`Elasticsearch`
- 文件存储：`MinIO`
- AI 接入：`Spring AI Alibaba`

### 3.2 初始化依赖建议
建议安装以下基础依赖：
- `spring-boot-starter-web`
- `spring-boot-starter-validation`
- `spring-boot-starter-security`
- `spring-boot-starter-actuator`
- `mybatis-plus-boot-starter`
- `postgresql`
- `spring-boot-starter-data-redis`
- `spring-boot-starter-amqp`
- `spring-boot-starter-aop`
- `spring-boot-starter-test`
- `spring-ai` 相关依赖
- `minio`
- `elasticsearch` 客户端依赖
- `jjwt` 或其他 JWT 方案依赖
- `lombok`
- `mapstruct`（可选）

---

## 4. 推荐目录结构
```text
src/main/java/com/noto/zhihui
├── ZhihuiApplication.java
├── common
│   ├── api
│   │   └── ApiResponse.java
│   ├── constants
│   ├── exception
│   │   ├── ErrorCode.java
│   │   ├── BizException.java
│   │   └── GlobalExceptionHandler.java
│   └── util
├── config
│   ├── MybatisPlusConfig.java
│   ├── RedisConfig.java
│   ├── RabbitMQConfig.java
│   ├── ElasticsearchConfig.java
│   ├── MinioConfig.java
│   ├── SecurityConfig.java
│   └── AiConfig.java
├── controller
├── dto
├── entity
├── enums
├── mapper
├── service
│   └── impl
├── vo
├── bo
├── security
├── ai
├── task
├── scheduler
├── mq
├── audit
└── util
```

---

## 5. 初始化必备配置文件

### 5.1 application.yml
建议作为统一入口配置，包含：
- 端口
- 数据源
- Redis
- RabbitMQ
- Elasticsearch
- MinIO
- AI 配置
- 日志配置
- 文件上传限制

### 5.2 application-dev.yml
开发环境配置：
- 本地数据库
- 本地 Redis
- 本地 RabbitMQ
- 本地 ES
- 本地 MinIO

### 5.3 application-test.yml
测试环境配置：
- 测试数据库
- 测试缓存
- 测试检索服务

### 5.4 application-prod.yml
生产环境配置：
- 生产数据库连接
- 生产缓存连接
- 生产消息队列
- 生产 AI 配置

---

## 6. 基础代码骨架建议

### 6.1 启动类
创建主启动类：
- `ZhihuiApplication.java`

### 6.2 通用返回结构
创建统一返回类：
- `ApiResponse.java`

### 6.3 错误处理
创建统一异常体系：
- `ErrorCode.java`
- `BizException.java`
- `GlobalExceptionHandler.java`

### 6.4 基础实体类
创建通用父类：
- `BaseEntity.java`

### 6.5 安全控制
创建基础安全配置：
- `SecurityConfig.java`
- `JwtUtil.java`
- `AuthFilter.java`

---

## 7. 核心模块初始化顺序
建议按以下顺序搭建后端模块：
1. 启动类与基础配置
2. 通用返回体与异常处理
3. 用户与鉴权模块
4. 笔记模块
5. 文件夹与标签模块
6. 搜索模块
7. AI 模块
8. 待办模块
9. 提醒模块
10. 审计日志模块
11. 异步任务模块

---

## 8. 第一个必须跑通的最小闭环
后端第一阶段必须让以下链路跑通：
1. 用户登录获取 token
2. 创建笔记
3. 保存笔记后生成索引任务
4. 搜索笔记
5. 调用 AI 生成总结
6. 提取待办
7. 创建提醒
8. 写入审计日志

---

## 9. MyBatis Plus 初始化建议

### 9.1 Mapper 结构
每张表对应一个 Mapper 接口：
- `UserMapper`
- `WorkspaceMapper`
- `NoteMapper`
- `TodoItemMapper`
- `ReminderMapper`
等

### 9.2 BaseMapper 使用原则
- 简单 CRUD 直接使用 BaseMapper
- 复杂查询写 XML 或自定义 Mapper 方法
- 统一分页插件配置

---

## 10. Redis 使用建议
Redis 第一版主要用于：
- 登录会话
- token 黑名单或刷新机制
- AI 会话缓存
- 草稿缓存
- 任务状态缓存
- 限流

---

## 11. RabbitMQ 使用建议
RabbitMQ 第一版主要用于：
- 文档索引异步构建
- AI 总结异步任务
- 待办提取异步任务
- 提醒触发异步分发
- 导入导出类任务

---

## 12. Elasticsearch 使用建议
第一版只做最核心的检索：
- 笔记标题索引
- 笔记正文索引
- 标签索引
- 命中文本高亮
- 结果排序

不要一开始做复杂聚合和复杂召回逻辑。

---

## 13. AI 初始化建议
### 13.1 Spring AI Alibaba 接入
建议先封装一层统一 AI 服务：
- 文本总结
- 文档问答
- 待办提取
- 智能体任务拆解

### 13.2 AI 服务原则
- 所有 AI 输出都必须可追溯
- AI 结果要可编辑、可确认
- 不直接覆盖用户原文

---

## 14. 后端健康检查接口
建议先提供：
- `/api/v1/health`
- `/api/v1/version`
- `/api/v1/actuator/health`

用于：
- 验证服务是否启动成功
- 验证依赖是否可连通

---

## 15. 开发顺序建议
建议后端按以下顺序开发：
1. 创建项目骨架
2. 配置基础依赖和环境
3. 完成统一返回和异常处理
4. 完成用户认证
5. 完成笔记基础 CRUD
6. 完成搜索索引与查询
7. 完成 AI 总结与问答
8. 完成待办与提醒
9. 完成审计日志
10. 完成异步任务链路

---

## 16. 最小可运行标准
后端工程初始化完成后，至少应满足：
- 项目可启动
- 配置可按环境切换
- 数据库可连接
- Redis 可连接
- 接口返回统一格式
- 异常可统一捕获
- 能返回健康检查结果

---

## 17. 结论
后端初始化的核心不是把所有业务一次性写完，而是先建立稳定的工程骨架、统一能力和模块边界，为后续功能开发提供可靠支撑。
