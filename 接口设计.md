# Noto · 知微 接口设计

## 1. 接口设计目标
本接口设计用于支撑 Noto · 知微 的 MVP 阶段能力，并为未来多端复用预留统一 API 结构。

### 设计原则
- 统一 REST 风格
- 统一响应格式
- 统一鉴权方式
- 业务模块分层清晰
- AI 能力通过独立接口暴露
- 所有关键操作可追踪

---

## 2. 通用约定

### 2.1 统一响应格式
```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "traceId": "xxx"
}
```

### 2.2 通用状态码
- `0`：成功
- `400`：参数错误
- `401`：未登录或 Token 失效
- `403`：无权限
- `404`：资源不存在
- `500`：服务器内部错误
- `1001`：AI 调用失败
- `1002`：搜索服务失败
- `1003`：异步任务失败

### 2.3 鉴权方式
- 使用 Token 鉴权
- 登录后返回 access token
- 未来可扩展 refresh token

### 2.4 分页参数
- `pageNo`
- `pageSize`

### 2.5 排序参数
- `sortBy`
- `sortOrder`

---

## 3. 用户与认证接口

### 3.1 用户注册
**POST** `/api/auth/register`

请求参数：
```json
{
  "username": "tom",
  "email": "tom@example.com",
  "password": "123456"
}
```

返回结果：
- 用户创建成功
- 返回登录态或提示登录

---

### 3.2 用户登录
**POST** `/api/auth/login`

请求参数：
```json
{
  "account": "tom@example.com",
  "password": "123456"
}
```

返回结果：
```json
{
  "token": "jwt-token",
  "user": {}
}
```

---

### 3.3 获取当前用户信息
**GET** `/api/auth/me`

返回结果：
- 用户基础信息
- 当前工作区信息

---

### 3.4 退出登录
**POST** `/api/auth/logout`

---

## 4. 工作区接口

### 4.1 获取当前工作区
**GET** `/api/workspaces/current`

### 4.2 创建工作区
**POST** `/api/workspaces`

### 4.3 更新工作区
**PUT** `/api/workspaces/{id}`

### 4.4 获取工作区列表
**GET** `/api/workspaces`

---

## 5. 笔记文件夹接口

### 5.1 新建文件夹
**POST** `/api/folders`

### 5.2 更新文件夹
**PUT** `/api/folders/{id}`

### 5.3 删除文件夹
**DELETE** `/api/folders/{id}`

### 5.4 文件夹列表
**GET** `/api/folders`

---

## 6. 笔记接口

### 6.1 创建笔记
**POST** `/api/notes`

请求参数：
```json
{
  "title": "笔记标题",
  "content": "markdown内容",
  "folderId": 1,
  "tagIds": [1, 2]
}
```

---

### 6.2 更新笔记
**PUT** `/api/notes/{id}`

---

### 6.3 删除笔记
**DELETE** `/api/notes/{id}`

---

### 6.4 获取笔记详情
**GET** `/api/notes/{id}`

返回结果：
- 标题
- 内容
- 总结
- 标签
- 附件
- 更新时间

---

### 6.5 笔记列表
**GET** `/api/notes`

支持参数：
- `keyword`
- `folderId`
- `tagId`
- `favorite`
- `pageNo`
- `pageSize`

---

### 6.6 收藏笔记
**POST** `/api/notes/{id}/favorite`

### 6.7 取消收藏
**DELETE** `/api/notes/{id}/favorite`

### 6.8 草稿保存
**POST** `/api/notes/{id}/draft`

---

## 7. 标签接口

### 7.1 创建标签
**POST** `/api/tags`

### 7.2 标签列表
**GET** `/api/tags`

### 7.3 删除标签
**DELETE** `/api/tags/{id}`

### 7.4 笔记绑定标签
**POST** `/api/notes/{id}/tags`

### 7.5 取消笔记标签
**DELETE** `/api/notes/{id}/tags/{tagId}`

---

## 8. 附件接口

### 8.1 上传附件
**POST** `/api/attachments/upload`

### 8.2 删除附件
**DELETE** `/api/attachments/{id}`

### 8.3 附件列表
**GET** `/api/notes/{noteId}/attachments`

---

## 9. 搜索接口

### 9.1 搜索笔记
**GET** `/api/search/notes`

支持参数：
- `keyword`
- `folderId`
- `tagId`
- `pageNo`
- `pageSize`

返回结果：
- 笔记列表
- 命中片段
- 高亮结果

---

### 9.2 搜索建议词
**GET** `/api/search/suggestions`

---

## 10. AI 接口

### 10.1 笔记总结
**POST** `/api/ai/notes/{id}/summary`

功能：
- 生成摘要
- 提炼结论
- 识别风险点

---

### 10.2 笔记问答
**POST** `/api/ai/notes/{id}/qa`

请求参数：
```json
{
  "question": "这篇笔记的核心结论是什么？"
}
```

返回结果：
- AI 回答
- 引用来源
- 命中片段

---

### 10.3 知识库问答
**POST** `/api/ai/workspace/qa`

请求参数：
```json
{
  "question": "我之前记录过哪些关于支付流程的内容？"
}
```

---

### 10.4 待办提取
**POST** `/api/ai/notes/{id}/extract-todos`

返回结果：
- 待办列表
- 截止时间
- 优先级
- 来源片段

---

### 10.5 相关文档推荐
**GET** `/api/ai/notes/{id}/related`

---

### 10.6 智能体任务执行
**POST** `/api/ai/agent/run`

请求参数：
```json
{
  "goal": "整理最近三篇项目文档，生成总结并提取待办"
}
```

返回结果：
- 任务步骤
- 执行状态
- 中间结果
- 用户确认项

---

## 11. 待办接口

### 11.1 创建待办
**POST** `/api/todos`

### 11.2 更新待办
**PUT** `/api/todos/{id}`

### 11.3 删除待办
**DELETE** `/api/todos/{id}`

### 11.4 待办详情
**GET** `/api/todos/{id}`

### 11.5 待办列表
**GET** `/api/todos`

支持参数：
- `status`
- `priority`
- `dueDate`
- `keyword`
- `pageNo`
- `pageSize`

### 11.6 完成待办
**POST** `/api/todos/{id}/complete`

### 11.7 取消完成
**POST** `/api/todos/{id}/reopen`

---

## 12. 提醒接口

### 12.1 创建提醒
**POST** `/api/reminders`

### 12.2 更新提醒
**PUT** `/api/reminders/{id}`

### 12.3 删除提醒
**DELETE** `/api/reminders/{id}`

### 12.4 提醒列表
**GET** `/api/reminders`

### 12.5 触发提醒记录
**POST** `/api/reminders/{id}/trigger`

---

## 13. AI 任务与日志接口

### 13.1 AI 任务列表
**GET** `/api/ai/tasks`

### 13.2 AI 任务详情
**GET** `/api/ai/tasks/{id}`

### 13.3 审计日志列表
**GET** `/api/audit/logs`

### 13.4 审计日志详情
**GET** `/api/audit/logs/{id}`

---

## 14. 文件上传与下载接口

### 14.1 上传文件
**POST** `/api/files/upload`

### 14.2 下载文件
**GET** `/api/files/{id}`

### 14.3 删除文件
**DELETE** `/api/files/{id}`

---

## 15. 前端页面对应接口映射
- 首页：工作区概览、待办提醒、最近笔记
- 笔记列表页：笔记列表、搜索、筛选
- 笔记详情页：笔记详情、AI 总结、待办提取、相关文档
- 笔记编辑页：创建、更新、草稿保存、附件上传
- AI 问答页：单笔记问答、全局问答、智能体运行
- 待办页：待办 CRUD、状态流转
- 提醒页：提醒 CRUD、触发记录
- 设置页：用户信息、偏好设置

---

## 16. 接口实现优先级

### 第一优先级
- 登录注册
- 笔记 CRUD
- 文件夹与标签
- 搜索接口
- 文档详情

### 第二优先级
- AI 总结
- AI 问答
- 待办提取
- 待办管理
- 提醒管理

### 第三优先级
- 智能体编排
- 审计日志
- 任务回放

---

## 17. 结论
本接口设计覆盖 Noto · 知微 MVP 所需的核心链路，并为后续多端和智能体能力扩展预留统一入口。下一步建议继续输出项目目录结构与后端实体类设计。
