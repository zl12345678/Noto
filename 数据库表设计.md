# Noto · 知微 数据库表设计

## 1. 设计目标
本数据库设计面向个人知识管理 + 行动清单 + AI 智能体能力，目标是：
- 支持笔记记录与分类
- 支持全文检索与 AI 问答
- 支持待办提取与提醒
- 支持后续扩展到多端与协作
- 保持结构清晰，便于后续拆分和扩展

---

## 2. 设计原则
1. 核心业务表尽量稳定，避免频繁变动
2. 业务表与 AI 生成数据分离，保证可追溯
3. 文档内容、索引内容、待办内容拆分存储
4. 所有关键动作都保留时间戳和审计记录
5. 预留多端通用字段，如 workspace、device、source 等

---

## 3. 核心实体关系概览
- 用户 `user`
- 工作区 `workspace`
- 笔记 `note`
- 笔记块 `note_block`
- 文件夹 `note_folder`
- 标签 `tag`
- 笔记标签关联 `note_tag`
- 附件 `note_attachment`
- AI 会话 `ai_chat_session`
- AI 消息 `ai_chat_message`
- 待办 `todo_item`
- 提醒 `reminder`
- 搜索索引任务 `search_index_task`
- AI 任务 `ai_task`
- 审计日志 `audit_log`
- 用户设置 `user_setting`

---

## 4. 表结构详细设计

## 4.1 用户表 `user`
### 用途
存储系统用户基础信息。

### 主要字段
- `id` bigint 主键
- `username` varchar 用户名
- `email` varchar 邮箱
- `password_hash` varchar 密码哈希
- `nickname` varchar 昵称
- `avatar_url` varchar 头像地址
- `status` tinyint 状态：正常、禁用
- `last_login_at` datetime 最后登录时间
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 作为整个系统的身份基础表
- 后续可扩展第三方登录字段

---

## 4.2 工作区表 `workspace`
### 用途
支持个人工作区，未来可扩展为团队空间。

### 主要字段
- `id` bigint 主键
- `owner_user_id` bigint 所属用户
- `name` varchar 工作区名称
- `type` tinyint 类型：个人、团队
- `description` varchar 描述
- `status` tinyint 状态
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- MVP 期默认每个用户一个个人工作区
- 后续可支持多人共享空间

---

## 4.3 笔记文件夹表 `note_folder`
### 用途
对笔记进行层级分类管理。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `parent_id` bigint 父文件夹 ID
- `name` varchar 文件夹名称
- `sort_order` int 排序
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 支持树形目录结构
- `parent_id` 为空表示顶层文件夹

---

## 4.4 笔记表 `note`
### 用途
存储用户的核心知识内容。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `folder_id` bigint 文件夹 ID
- `title` varchar 笔记标题
- `content` longtext 笔记正文
- `content_type` varchar 内容类型：markdown、richtext
- `summary` text AI 总结摘要
- `status` tinyint 状态：正常、归档、删除
- `is_favorite` tinyint 是否收藏
- `last_edited_at` datetime 最后编辑时间
- `created_by` bigint 创建人
- `updated_by` bigint 更新人
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 这是最核心的业务表
- `content` 保存原始内容
- `summary` 保存 AI 或人工总结结果
- 后续可扩展版本号字段

---

## 4.5 笔记块表 `note_block`
### 用途
存储笔记被切分后的内容块，供检索和 AI 召回使用。

### 主要字段
- `id` bigint 主键
- `note_id` bigint 笔记 ID
- `block_index` int 块序号
- `block_type` varchar 块类型：text、heading、list、code、table
- `content` text 块内容
- `token_count` int token 估算数
- `embedding_id` varchar 向量引用 ID
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 用于 RAG 召回
- 用于搜索结果定位
- 建议异步生成

---

## 4.6 标签表 `tag`
### 用途
统一管理标签。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `name` varchar 标签名
- `color` varchar 标签颜色
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

---

## 4.7 笔记标签关联表 `note_tag`
### 用途
记录笔记与标签的多对多关系。

### 主要字段
- `id` bigint 主键
- `note_id` bigint 笔记 ID
- `tag_id` bigint 标签 ID
- `created_at` datetime 创建时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 建议 `(note_id, tag_id)` 唯一约束

---

## 4.8 附件表 `note_attachment`
### 用途
存储笔记中的图片、文件等附件信息。

### 主要字段
- `id` bigint 主键
- `note_id` bigint 笔记 ID
- `file_name` varchar 文件名
- `file_type` varchar 文件类型
- `file_size` bigint 文件大小
- `file_url` varchar 文件地址
- `storage_key` varchar 存储对象键
- `created_at` datetime 创建时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 文件实际存储在 MinIO
- 数据库只保存元数据和访问路径

---

## 4.9 AI 会话表 `ai_chat_session`
### 用途
记录用户与 AI 的会话上下文。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `user_id` bigint 用户 ID
- `session_type` varchar 会话类型：note_qa、workspace_qa、agent_task
- `title` varchar 会话标题
- `latest_message_at` datetime 最近消息时间
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 支持按会话进行历史追踪
- 方便后续实现多轮问答

---

## 4.10 AI 消息表 `ai_chat_message`
### 用途
存储会话内的每条消息。

### 主要字段
- `id` bigint 主键
- `session_id` bigint 会话 ID
- `role` varchar 角色：user、assistant、system、tool
- `content` longtext 消息内容
- `token_usage` int token 数
- `model_name` varchar 模型名称
- `referenced_note_ids` varchar 关联笔记 ID 列表
- `created_at` datetime 创建时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 可用于回放对话历史
- 可用于优化提示词和评估效果

---

## 4.11 待办表 `todo_item`
### 用途
存储从笔记中提取或人工创建的待办事项。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `note_id` bigint 来源笔记 ID
- `title` varchar 待办标题
- `description` text 待办描述
- `priority` tinyint 优先级：低、中、高、紧急
- `status` tinyint 状态：未开始、进行中、已完成、已过期
- `due_at` datetime 截止时间
- `completed_at` datetime 完成时间
- `created_by` bigint 创建人
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 支持 AI 自动提取
- 支持用户手动创建
- `note_id` 为空表示独立待办

---

## 4.12 提醒表 `reminder`
### 用途
存储待办或任务的提醒规则和触发结果。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `todo_id` bigint 待办 ID
- `reminder_type` varchar 提醒类型：once、daily、weekly、custom
- `trigger_at` datetime 触发时间
- `message` varchar 提醒内容
- `status` tinyint 状态：待触发、已触发、已取消
- `sent_at` datetime 实际发送时间
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 第一版可仅实现站内提醒
- 后续可扩展到邮箱、飞书、企微

---

## 4.13 搜索索引任务表 `search_index_task`
### 用途
记录笔记索引构建的异步任务。

### 主要字段
- `id` bigint 主键
- `note_id` bigint 笔记 ID
- `task_type` varchar 任务类型：index_note、reindex_note、delete_index
- `status` tinyint 状态：待处理、处理中、成功、失败
- `retry_count` int 重试次数
- `error_message` varchar 错误信息
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 用于异步构建 ES 索引
- 也可用于排查索引失败问题

---

## 4.14 AI 任务表 `ai_task`
### 用途
记录 AI 总结、问答、待办提取、智能体执行等异步任务。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `user_id` bigint 用户 ID
- `task_type` varchar 任务类型：summarize、qa、extract_todo、agent_flow
- `target_id` bigint 目标对象 ID，例如 note_id
- `status` tinyint 状态：待处理、处理中、成功、失败
- `input_content` text 输入内容
- `output_content` longtext 输出内容
- `retry_count` int 重试次数
- `error_message` varchar 错误信息
- `created_at` datetime 创建时间
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 可用于记录 AI 任务全过程
- 便于做可观测性和任务回放

---

## 4.15 审计日志表 `audit_log`
### 用途
记录关键操作行为，便于排查问题和追踪系统执行过程。

### 主要字段
- `id` bigint 主键
- `workspace_id` bigint 工作区 ID
- `user_id` bigint 用户 ID
- `action_type` varchar 动作类型：login、note_edit、ai_call、todo_create、reminder_trigger
- `resource_type` varchar 资源类型
- `resource_id` bigint 资源 ID
- `detail` text 详细描述
- `trace_id` varchar 链路追踪 ID
- `created_at` datetime 创建时间

### 说明
- 只记录，不做业务依赖
- 便于排错、统计和审计

---

## 4.16 用户设置表 `user_setting`
### 用途
存储用户偏好配置。

### 主要字段
- `id` bigint 主键
- `user_id` bigint 用户 ID
- `setting_key` varchar 配置键
- `setting_value` text 配置值
- `updated_at` datetime 更新时间
- `deleted` tinyint 逻辑删除标记

### 说明
- 可用于保存提醒偏好、AI 偏好、界面偏好等

---

## 5. 索引设计建议

### 5.1 user 表
- `email` 唯一索引
- `username` 唯一索引

### 5.2 note 表
- `workspace_id`
- `folder_id`
- `title`
- `last_edited_at`

### 5.3 note_block 表
- `note_id`
- `block_index`

### 5.4 tag 表
- `(workspace_id, name)` 唯一索引

### 5.5 todo_item 表
- `workspace_id`
- `status`
- `due_at`

### 5.6 reminder 表
- `status`
- `trigger_at`

### 5.7 ai_task 表
- `status`
- `task_type`
- `target_id`

### 5.8 audit_log 表
- `user_id`
- `action_type`
- `created_at`
- `trace_id`

---

## 6. 逻辑删除与审计

### 6.1 逻辑删除
除少数日志类表外，大部分核心表都建议使用逻辑删除字段 `deleted`，避免误删导致数据丢失。

### 6.2 审计记录
凡是以下操作都建议写入审计日志：
- 登录
- 新建或修改笔记
- AI 调用
- 待办生成
- 提醒触发
- 任务失败

---

## 7. 与 AI 的关系

### 7.1 AI 直接读取的数据
- note
- note_block
- todo_item
- ai_chat_message

### 7.2 AI 生成的数据
- note.summary
- todo_item
- ai_task.output_content
- ai_chat_message.content

### 7.3 AI 不直接修改的数据
- 原始笔记正文 `note.content`
- 审计日志 `audit_log`

### 7.4 设计原则
AI 生成结果应可编辑、可确认、可追溯，不应直接覆盖用户原始内容。

---

## 8. MVP 阶段建议最先落地的表
MVP 第一版建议优先实现以下表：
- `user`
- `workspace`
- `note_folder`
- `note`
- `note_block`
- `tag`
- `note_tag`
- `note_attachment`
- `ai_chat_session`
- `ai_chat_message`
- `todo_item`
- `reminder`
- `search_index_task`
- `ai_task`
- `audit_log`
- `user_setting`

---

## 9. 后续扩展建议
后续如需扩展团队协作，可增加：
- `workspace_member`
- `document_permission`
- `note_version`
- `comment`
- `share_link`
- `activity_feed`

---

## 10. 结论
本数据库设计以个人知识管理 + 行动清单 + AI 智能体为中心，采用模块化表结构，兼顾 MVP 快速落地与后续多端扩展。下一步建议基于本设计继续输出接口清单与后端实体类设计。
