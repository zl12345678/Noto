# Noto · 知微 项目目录结构

## 1. 目录设计目标
本目录结构面向 Web 优先、未来多端通用的产品形态，目标是：
- 代码结构清晰
- 业务模块边界明确
- 便于后续扩展 App、小程序、桌面端
- 便于 AI 能力、检索能力、任务能力拆分
- 适合 Java + Vue 的工程化开发方式

---

## 2. 推荐仓库结构
建议采用前后端分离的单仓库结构：

```text
noto-zhihui/
├── backend/
└── frontend/
```

也可以后续拆成独立仓库，但 MVP 阶段单仓库更方便管理。

---

## 3. 后端目录结构

```text
backend/
├── src/
│   ├── main/
│   │   ├── java/com/noto/zhihui/
│   │   │   ├── ZhihuiApplication.java
│   │   │   ├── common/
│   │   │   ├── config/
│   │   │   ├── controller/
│   │   │   ├── dto/
│   │   │   ├── vo/
│   │   │   ├── bo/
│   │   │   ├── entity/
│   │   │   ├── enums/
│   │   │   ├── mapper/
│   │   │   ├── service/
│   │   │   ├── service/impl/
│   │   │   ├── ai/
│   │   │   ├── task/
│   │   │   ├── security/
│   │   │   ├── util/
│   │   │   ├── handler/
│   │   │   └── interceptor/
│   │   └── resources/
│   │       ├── application.yml
│   │       ├── application-dev.yml
│   │       ├── application-prod.yml
│   │       ├── mapper/
│   │       ├── logback-spring.xml
│   │       └── db/migration/
│   └── test/
│       └── java/com/noto/zhihui/
├── pom.xml
└── README.md
```

---

## 4. 后端分层说明

### 4.1 common
公共返回体、分页、常量、通用异常、通用工具类。

### 4.2 config
系统配置类，例如：
- MyBatis Plus 配置
- Redis 配置
- Elasticsearch 配置
- RabbitMQ 配置
- Spring AI 配置
- MinIO 配置
- Web MVC 配置

### 4.3 controller
对外暴露 REST API 的控制层。
建议按业务模块分包：
- auth
- user
- note
- folder
- tag
- search
- ai
- todo
- reminder
- audit

### 4.4 dto
前端请求参数对象。

### 4.5 vo
后端响应对象。

### 4.6 bo
业务层内部对象，用于封装复杂业务过程。

### 4.7 entity
数据库实体类，和表结构一一对应。

### 4.8 enums
业务枚举类。

### 4.9 mapper
MyBatis Plus Mapper 接口。

### 4.10 service / service.impl
业务服务接口与实现类。

### 4.11 ai
AI 相关能力封装：
- prompt 组织
- RAG 检索
- 模型调用
- 结果后处理
- 工具调用
- 智能体编排

### 4.12 task
异步任务与定时任务：
- 索引构建
- 提醒触发
- AI 任务轮询
- 任务重试

### 4.13 security
鉴权与安全相关：
- JWT
- 用户上下文
- 权限校验
- 过滤器

### 4.14 handler
全局异常处理、统一响应处理、字段处理器。

### 4.15 interceptor
请求拦截器，例如 traceId、日志、权限相关拦截。

---

## 5. 前端目录结构

```text
frontend/
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── composables/
│   ├── config/
│   ├── layouts/
│   ├── pages/
│   ├── router/
│   ├── store/
│   ├── styles/
│   ├── types/
│   ├── utils/
│   ├── views/
│   │   ├── auth/
│   │   ├── dashboard/
│   │   ├── notes/
│   │   ├── search/
│   │   ├── ai/
│   │   ├── todos/
│   │   ├── reminders/
│   │   └── settings/
│   ├── App.vue
│   └── main.ts
├── public/
├── index.html
├── vite.config.ts
├── package.json
└── README.md
```

---

## 6. 前端目录说明

### 6.1 api
统一封装后端接口请求。

### 6.2 assets
图片、静态资源、图标等。

### 6.3 components
通用组件，例如：
- 编辑器包装组件
- 搜索框
- 标签选择器
- 待办卡片
- AI 回答卡片
- 加载状态组件

### 6.4 composables
Vue 组合式函数，例如：
- useAuth
- useNote
- useSearch
- useAI

### 6.5 layouts
页面布局，例如：
- 主布局
- 登录布局
- 文档编辑布局

### 6.6 pages / views
页面级组件。

### 6.7 router
前端路由配置。

### 6.8 store
状态管理，建议使用 Pinia。

### 6.9 styles
全局样式、主题变量、主题配置。

### 6.10 types
前端类型定义，与 API 返回结构对齐。

### 6.11 utils
通用工具函数。

---

## 7. 页面级目录建议

### 7.1 auth
- login
- register

### 7.2 dashboard
- 首页仪表盘
- 最近编辑
- 收藏笔记
- 待办概览
- 提醒概览

### 7.3 notes
- 笔记列表
- 笔记编辑页
- 笔记详情页
- 文件夹管理
- 标签管理

### 7.4 search
- 搜索页
- 搜索结果页

### 7.5 ai
- AI 问答页
- AI 总结页
- AI 任务记录页

### 7.6 todos
- 待办列表
- 待办详情
- 待办状态管理

### 7.7 reminders
- 提醒列表
- 提醒配置

### 7.8 settings
- 个人资料
- 通知设置
- AI 偏好设置

---

## 8. 路由设计建议

### 8.1 登录前路由
- `/login`
- `/register`

### 8.2 登录后主路由
- `/`
- `/notes`
- `/notes/:id`
- `/notes/edit/:id`
- `/search`
- `/ai`
- `/todos`
- `/reminders`
- `/settings`

### 8.3 路由守卫
- 未登录跳转登录页
- 登录后禁止再次访问登录页
- 权限不足时给出提示页

---

## 9. 模块边界建议

### 9.1 后端模块边界
建议按业务边界拆分，而不是按技术层混在一起：
- auth 模块
- note 模块
- search 模块
- ai 模块
- todo 模块
- reminder 模块
- audit 模块

### 9.2 前端模块边界
建议页面组件、通用组件、API、状态管理分离，不要把逻辑全写进页面。

---

## 10. 多端通用预留

### 10.1 后端统一 API
Web、App、小程序、桌面端都共用同一套 API。

### 10.2 前端形态隔离
Web 端只负责当前页面呈现，不把业务逻辑放在前端。

### 10.3 类型同步
建议统一维护接口类型，后续多端共用。

---

## 11. 配置文件建议

### 11.1 后端配置
- `application.yml`
- `application-dev.yml`
- `application-prod.yml`

### 11.2 前端配置
- `.env.development`
- `.env.production`

---

## 12. 日志与监控目录建议
可在后端预留：
- `log/`
- `monitor/`
- `trace/`

用于后续接入链路追踪、调用日志和异常排查。

---

## 13. 初始化优先级建议

### 第一优先级
- 后端项目骨架
- 前端项目骨架
- 基础目录结构
- 配置文件
- 登录注册页面

### 第二优先级
- 笔记模块
- 搜索模块
- AI 模块

### 第三优先级
- 待办模块
- 提醒模块
- 审计模块

---

## 14. 结论
本项目建议采用前后端分离、模块化单体的目录结构，既适合 MVP 快速开发，也为后续多端扩展和模块拆分提供清晰边界。
