# Noto · 知微 前端启动脚手架代码结构

> 说明：以下内容用于前端 Web 工程启动阶段的代码骨架参考。实际开发时可以按模块逐步创建对应文件。

---

## 1. 项目入口

```ts
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import { createPinia } from 'pinia'
import Antd from 'ant-design-vue'
import 'ant-design-vue/dist/reset.css'
import './styles/global.less'

const app = createApp(App)

app.use(createPinia())
app.use(router)
app.use(Antd)
app.mount('#app')
```

---

## 2. 根组件

```vue
<template>
  <router-view />
</template>
```

---

## 3. 路由初始化

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/login', component: () => import('@/views/auth/LoginView.vue') },
    { path: '/register', component: () => import('@/views/auth/RegisterView.vue') },
    { path: '/home', component: () => import('@/views/home/HomeView.vue') },
    { path: '/notes', component: () => import('@/views/notes/NotesListView.vue') },
    { path: '/notes/:id', component: () => import('@/views/notes/NoteDetailView.vue') },
    { path: '/editor/:id?', component: () => import('@/views/notes/NoteEditorView.vue') },
    { path: '/search', component: () => import('@/views/search/SearchView.vue') },
    { path: '/ai', component: () => import('@/views/ai/AiChatView.vue') },
    { path: '/todos', component: () => import('@/views/todos/TodoListView.vue') },
    { path: '/reminders', component: () => import('@/views/reminders/ReminderListView.vue') },
    { path: '/settings', component: () => import('@/views/settings/SettingsView.vue') },
    { path: '/:pathMatch(.*)*', redirect: '/home' }
  ]
})

export default router
```

---

## 4. 全局状态管理初始化

### 4.1 用户状态
```ts
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    userInfo: null,
    token: ''
  }),
  actions: {
    setUserInfo(userInfo: any) {
      this.userInfo = userInfo
    },
    setToken(token: string) {
      this.token = token
    },
    clearAuth() {
      this.userInfo = null
      this.token = ''
    }
  }
})
```

### 4.2 工作区状态
```ts
import { defineStore } from 'pinia'

export const useWorkspaceStore = defineStore('workspace', {
  state: () => ({
    currentWorkspace: null
  }),
  actions: {
    setCurrentWorkspace(workspace: any) {
      this.currentWorkspace = workspace
    }
  }
})
```

### 4.3 笔记状态
```ts
import { defineStore } from 'pinia'

export const useNoteStore = defineStore('note', {
  state: () => ({
    currentNote: null,
    noteList: [],
    folderTree: []
  })
})
```

---

## 5. 请求封装

```ts
import axios from 'axios'

const request = axios.create({
  baseURL: '/api/v1',
  timeout: 15000
})

request.interceptors.request.use((config) => {
  const token = localStorage.getItem('token')
  if (token && config.headers) {
    config.headers.Authorization = `Bearer ${token}`
  }
  config.headers['X-Trace-Id'] = crypto.randomUUID()
  return config
})

request.interceptors.response.use(
  (response) => response.data,
  (error) => Promise.reject(error)
)

export default request
```

---

## 6. API 模块结构

```text
src/api
├── authApi.ts
├── userApi.ts
├── noteApi.ts
├── searchApi.ts
├── aiApi.ts
├── todoApi.ts
├── reminderApi.ts
└── auditApi.ts
```

### 示例：noteApi
```ts
import request from '@/utils/request'

export const noteApi = {
  createNote(data: any) {
    return request.post('/notes', data)
  },
  updateNote(id: number, data: any) {
    return request.put(`/notes/${id}`, data)
  },
  getNoteDetail(id: number) {
    return request.get(`/notes/${id}`)
  },
  deleteNote(id: number) {
    return request.delete(`/notes/${id}`)
  },
  listNotes(params: any) {
    return request.get('/notes', { params })
  }
}
```

---

## 7. 布局结构

### 7.1 主布局
```vue
<template>
  <a-layout class="main-layout">
    <a-layout-sider width="240">
      <!-- 左侧导航 -->
    </a-layout-sider>
    <a-layout>
      <a-layout-header>
        <!-- 顶部工具栏 -->
      </a-layout-header>
      <a-layout-content>
        <router-view />
      </a-layout-content>
    </a-layout>
    <a-layout-sider width="360" theme="light">
      <!-- AI 面板 / 待办面板 -->
    </a-layout-sider>
  </a-layout>
</template>
```

---

## 8. 页面骨架建议

### 8.1 登录页
- 账号输入
- 密码输入
- 登录按钮
- 注册入口

### 8.2 注册页
- 用户名
- 邮箱
- 密码
- 确认密码

### 8.3 首页
- 搜索入口
- 最近笔记
- 收藏笔记
- 待办摘要
- 提醒摘要
- AI 快捷入口

### 8.4 笔记列表页
- 文件夹树
- 标签筛选
- 列表展示
- 新建按钮

### 8.5 笔记编辑页
- 标题输入
- TipTap 编辑器
- 保存按钮
- AI 总结按钮
- 待办提取按钮

### 8.6 笔记详情页
- 笔记正文
- 总结卡片
- 相关笔记推荐
- 关联待办

### 8.7 AI 问答页
- 问题输入
- 对话列表
- 引用来源展示
- 流式回答区域

### 8.8 待办页
- 待办列表
- 状态筛选
- 优先级筛选
- 到期提醒

### 8.9 提醒页
- 提醒列表
- 提醒状态
- 新建提醒入口

### 8.10 设置页
- 用户信息
- 工作区设置
- AI 偏好
- 提醒偏好

---

## 9. 组件目录建议

```text
src/components
├── common
│   ├── AppHeader.vue
│   ├── AppSidebar.vue
│   ├── EmptyState.vue
│   ├── LoadingSkeleton.vue
│   └── PageTitle.vue
└── business
    ├── NoteTree.vue
    ├── NoteList.vue
    ├── NoteEditor.vue
    ├── AiPanel.vue
    ├── TodoPanel.vue
    └── ReminderCard.vue
```

---

## 10. 样式建议

### 10.1 全局样式
- 统一页面背景色
- 统一间距体系
- 统一标题层级
- 统一按钮样式

### 10.2 主题风格
- 以简洁、克制、知识感为主
- 避免过度花哨
- 保留较强的阅读和编辑体验

---

## 11. TipTap 组件初始化建议

```vue
<template>
  <EditorContent :editor="editor" />
</template>

<script setup lang="ts">
import { Editor, EditorContent } from '@tiptap/vue-3'
import StarterKit from '@tiptap/starter-kit'

const editor = new Editor({
  extensions: [StarterKit],
  content: '<p>请输入内容...</p>'
})
</script>
```

---

## 12. 启动阶段建议先补齐的前端文件
- `main.ts`
- `App.vue`
- `router/index.ts`
- `stores/user.ts`
- `utils/request.ts`
- `layouts/MainLayout.vue`
- `views/auth/LoginView.vue`
- `views/home/HomeView.vue`
- `views/notes/NotesListView.vue`
- `views/notes/NoteEditorView.vue`

---

## 13. 前端第一阶段最小可运行标准
前端初始化完成后，至少应满足：
- 项目可启动
- 路由可跳转
- 登录页可显示
- 主布局可显示
- 请求封装可用
- Pinia 可用
- TipTap 可挂载
- 基础样式可显示

---

## 14. 结论
前端启动脚手架的核心目标是先建立统一的页面布局、路由体系、状态管理和请求封装，让后续业务页面可以快速填充，而不是一开始就追求复杂功能。
